name: Generate Snake

on:
 schedule:
   - cron: "0 0 * * *"

jobs:
 build:
   runs-on: ubuntu-latest
   steps:
     - uses: Platane/snk/svg-only@v3
       with:
         github_user_name: somansinghal
         outputs: |
           dist/github-contribution-grid-snake.svg
